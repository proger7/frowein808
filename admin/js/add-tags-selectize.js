jQuery(document).ready(function($) {

	// $("input[name='save[product_tags]").selectize();
	// $("select[name='save[product_tags]'").attr('multiple','multiple');
	$("select[name='save[product_tags][]'").selectize();

});